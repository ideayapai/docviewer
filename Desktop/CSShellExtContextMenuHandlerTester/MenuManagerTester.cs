using CSShellExtContextMenuHandler;
using NUnit.Framework;

namespace CSShellExtContextMenuHandlerTester
{
    [TestFixture]
    public class MenuManagerTester
    {
        [Test]
        public void should_get_menuInfo()
        {
            //var result = MenuManager.Instance.GetMenuItemInfo();
            //Assert.AreEqual(result.Count, 0);
        }

        [Test]
        public void should_get_menuInfo_after_register()
        {
            //MenuManager.Instance.Register(new MenuContext
            //{
            //    IdCmdFirst = 0,
            //});

            //var result = MenuManager.Instance.GetMenuItemInfo();
            //Assert.AreEqual(result.Count, 3);
        }
    }
}
